_c5b‍.x([["errorResponseHandler",()=>errorResponseHandler],["invalidRouteHandler",()=>invalidRouteHandler]]);const errorResponseHandler =(err,req,res,next) => {
    const statusCode = err.statusCode ||400;
    res.status(statusCode).json({
        message:err.message,
        stack: process.env.NODE_ENV === "production" ? null : err.stack
    })
}

       const invalidRouteHandler = (req,res,next) => {
    let error= new Error("Invalid Route");
    error.statusCode=404;
    next(error);
}