let fs,path;_b7d‍.x([["fileRemover",()=>fileRemover]]);_b7d‍.w("fs",[["default",["fs"],function(v){fs=v}]]);_b7d‍.w("path",[["default",["path"],function(v){path=v}]]);//fileSystem Module





const fileRemover =(filename) => {
    fs.unlink(path.join(_dirname,'../uploads',filename),function(err) {
        if(err && err.code=="ENOENT"){
            //file doens't exist
           console.log(`File ${filename} Doesn't exist ,Won't remove it `)
        }
        else {
            if (err) {
                console.log(`Error While DELETING ${filename} `)
            }
            else {
                console.log(`${filename} Removed Successfully `)
            }
        }
    })
    
}

