let express,dotenv;_4ba‍.w("express",[["default",["express"],function(v){express=v}]]);_4ba‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);


dotenv.config();
const app = express();
app.use(express.json());

app.get("/",(req,res) => {
    res.send("Server is Running")
});
app.listen(process.env.PORT,() => {
    console.log(`Server is Running On Port ${p}`);

})
